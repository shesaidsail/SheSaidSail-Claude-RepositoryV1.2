version: "3.9"

# Weather Market Edge Tracker — production compose file
# All services share one SQLite volume (data/) and logs volume (logs/).
#
# Usage:
#   docker compose up -d          # start everything
#   docker compose logs -f        # tail all logs
#   docker compose restart        # restart all services
#   docker compose down           # stop (data persists in volumes)
#   docker compose ps             # check service health

x-common: &common
  build: .
  env_file: .env
  restart: unless-stopped
  volumes:
    - wx_data:/app/data
    - wx_logs:/app/logs
    - wx_backups:/app/backups

services:

  # ── Streamlit dashboard ──────────────────────────────────────────────────
  dashboard:
    <<: *common
    container_name: wx_dashboard
    command:
      - streamlit
      - run
      - dashboard/app.py
      - --server.port=8501
      - --server.address=0.0.0.0
      - --server.headless=true
      - --server.enableCORS=false
    ports:
      - "8501:8501"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8501/_stcore/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s

  # ── Scanner + paper-trade worker (runs every 2 min) ─────────────────────
  scanner:
    <<: *common
    container_name: wx_scanner
    command: ["python", "scripts/scheduler.py"]
    depends_on:
      dashboard:
        condition: service_healthy

  # ── Settlement worker (runs once per hour, settles yesterday's trades) ──
  settlement:
    <<: *common
    container_name: wx_settlement
    command: ["python", "scripts/settle_worker.py"]
    depends_on:
      dashboard:
        condition: service_healthy

  # ── CLV snapshot + stale-trade void worker ───────────────────────────────
  clv_worker:
    <<: *common
    container_name: wx_clv
    command: ["python", "scripts/clv_worker.py"]
    depends_on:
      dashboard:
        condition: service_healthy

  # ── Daily report + alert worker ─────────────────────────────────────────
  daily_report:
    <<: *common
    container_name: wx_daily_report
    command: ["python", "scripts/daily_report.py"]
    depends_on:
      dashboard:
        condition: service_healthy

volumes:
  wx_data:
  wx_logs:
  wx_backups:
